"use client";
import { FormEvent, useEffect, useState } from "react";
export default function Login(){const[setup,setSetup]=useState<boolean|null>(null);const[error,setError]=useState("");const[form,setForm]=useState({fullName:"Nguyễn Hữu Cảnh",username:"",password:""});useEffect(()=>{fetch("/api/auth/status").then(r=>r.json()).then(d=>{if(d.user)location.href="/";else if(d.error)setError(d.error);else setSetup(d.setupRequired)}).catch(()=>setError("Không kết nối được máy chủ"));},[]);async function submit(e:FormEvent){e.preventDefault();setError("");const url=setup?"/api/auth/setup":"/api/auth/login";const r=await fetch(url,{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify(form)});const d=await r.json();if(r.ok)location.href="/";else setError(d.error||"Có lỗi xảy ra");}return <main className="auth-page"><section className="auth-brand"><div className="auth-logo">H</div><p>HẢI SẢN CẢNG PHAN THIẾT</p><h1>Điều hành kinh doanh<br/>trong một nơi duy nhất.</h1><span>Đơn hàng · Doanh số · Báo cáo · Công văn · Lịch làm việc</span></section><form className="auth-card" onSubmit={submit}><span className="eyebrow">CỔNG NỘI BỘ</span><h2>{setup?"Thiết lập tài khoản Giám đốc":"Đăng nhập hệ thống"}</h2><p>{setup?"Tài khoản đầu tiên có toàn quyền quản trị.":"Sử dụng tài khoản do Giám đốc hoặc Admin cấp."}</p>{setup&&<label>Họ và tên<input required value={form.fullName} onChange={e=>setForm({...form,fullName:e.target.value})}/></label>}<label>Tên đăng nhập<input required autoComplete="username" value={form.username} onChange={e=>setForm({...form,username:e.target.value})} placeholder="VD: canh.nguyen"/></label><label>Mật khẩu<input required type="password" minLength={6} autoComplete={setup?"new-password":"current-password"} value={form.password} onChange={e=>setForm({...form,password:e.target.value})} placeholder="Tối thiểu 6 ký tự"/></label>{error&&<div className="auth-error">{error}</div>}<button disabled={setup===null}>{setup===null?"Đang kiểm tra...":setup?"Tạo tài khoản & bắt đầu":"Đăng nhập"}</button><small>Liên hệ Admin nếu quên mật khẩu.</small></form></main>}
