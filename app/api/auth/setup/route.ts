import { createSession, hashPassword } from "../../../../server/auth";
import { query } from "../../../../server/db";
export async function POST(req:Request){try{const count=await query<{count:string}>("SELECT COUNT(*)::text AS count FROM users");if(Number(count.rows[0].count)>0)return Response.json({error:"Hệ thống đã được thiết lập"},{status:409});const p=await req.json() as {fullName?:string;username?:string;password?:string};if(!p.fullName||!p.username||!p.password||p.password.length<6)return Response.json({error:"Vui lòng nhập đủ thông tin; mật khẩu tối thiểu 6 ký tự"},{status:400});const pass=await hashPassword(p.password);const r=await query<{id:number}>("INSERT INTO users(username,full_name,role,password_hash,password_salt,must_change_password) VALUES($1,$2,'director',$3,$4,FALSE) RETURNING id",[p.username.toLowerCase().trim(),p.fullName.trim(),pass.hash,pass.salt]);await createSession(r.rows[0].id);return Response.json({ok:true});}catch(e){return Response.json({error:e instanceof Error?e.message:"Không thể thiết lập"},{status:500});}}
